{
  "name": "user-subscriber-api",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server/main.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "seed": "node server/seeders.js"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  },
  "dependencies": {
    "url": "^0.11.0"
  }
}
